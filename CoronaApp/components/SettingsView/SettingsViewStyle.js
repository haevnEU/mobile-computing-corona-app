import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container:{
        display: "flex",
        flexDirection: "row",
        alignItems: "center"
    },

    text:{
        color: "#ffffff",
        fontSize:25
    },

    input: {
    },

    switch:{
        color: "#ff00ff",
    }



});
